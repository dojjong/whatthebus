package what.the.bus.noticeComment.service;

import what.the.bus.noticeComment.NoticeCommentVO;

public interface UpdateNoticeCommentService {
	void updateNoticeComment(NoticeCommentVO vo);
	
}
